server:
  port: 9000

eureka:
  instance:
      hostname: 148.70.5.68
  client:
    serviceUrl:
      defaultZone: http://148.70.5.68:8761/eureka/

spring:
  application:
    name: cloudDemo
  cloud:
    config:
      profile: test
      discovery:
        enabled: true
        service-id: config-server
      label: master
